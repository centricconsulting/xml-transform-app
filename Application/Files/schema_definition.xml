<?xml version="1.0" encoding="UTF-8"?>
<model>
  
  <instruction
    multi-byte="false"   
    column-case="lower"
    column-whitespace-replace = "_"
    />

  <entity
    name="Customer"
    definition="Organization or individual that has ever placed a sales order."
    class="master" physical="Customer">

      <!-- GRAIN attributeS -->
      <attribute
        name ="Customer"
        grain="true"
        class="reference" physical="Customer ." 
        data-type="text" data-length="200" 
        required="true" />

      <!-- FOREIGN KEY attributeS -->
      
      <!-- ATTRIBUTE attributeS -->
      <attribute
        name ="Customer Name"
        definition="Name of the customer organization."
        class="name" physical="Customer Legal ."
        data-type="text" data-length="200"
        required="true"
        display="true" />

      <attribute
        name ="Parent Organization Name"
        definition="Name of a parent organization, often owning multiple customers."
        class="name" physical="Parent Organization ."
        data-type="text" data-length="200"
        required="true"
        display="true" />

      <attribute 
        name ="Customer Number"
        definition="Identifier of the customer created by internal systems.  Not necessarily unique."
        physical="Customer ."
        class="number"
        data-type="text" 
        data-length="200" 
        display="true" />

      <attribute
        name ="Internal Risk Score"
        definition="Rating of risk that the customer will pay invoices by the specified term.  Higher score indicates greater risk."
        class="value" physical="Risk Score ."
        data-type="decimal" data-length="12" data-precision="4"
        display="true" />

  </entity>

  <entity
    name="Customer Xref"
    definition="Cross-reference for customers"
    class="xref" physical="Customer .">

    <!-- GRAIN attributeS -->
    <attribute
      name ="Customer"
      grain="true"
      class="reference" physical="Customer ."
      data-type="text" data-length="200"
      required="true" />

    <!-- FOREIGN KEY attributeS -->
    <attribute
      name ="Customer"
      class="reference" physical="Master Customer ."
      data-type="text" data-length="200"
      required="false" />

    
  </entity>

  <entity
    name="Legal Entity Holiday"
    definition="Holiday dates specified for a legal entity."
    class="master">

      <!-- GRAIN attributeS -->
      <attribute
        name ="Legal Entity"
        grain="true"
        class="reference" physical="Legal Entity ." 
        data-type="text" data-length="200" 
        required="true" />

      <attribute
        name ="Fiscal Year"
        definition ="Fiscal year corresponding to the legal entity."
        grain="true"
        class="integer"
        data-type="integer"
        required="true" />

      <attribute
        name ="Fiscal Period Of Year"
        definition ="Index from 1..n where (n) is the number of periods in a Fiscal Year."
        grain="true"
        class="integer"
        data-type="integer"
        required="true" />

      <!-- FOREIGN KEY attributeS -->
      
      <!-- ATTRIBUTE attributeS -->
      <attribute
        name ="Begin Fiscal Period Date"
        definition="Date of the first day in the corresponding fiscal period."
        class="date" physical="Begin Fiscal Period ."
        data-type="date"
        required="true"
        display="true" />

      <attribute
        name ="End Fiscal Period Date"
        definition="Date of the last day in the corresponding fiscal period."
        class="date" physical="End Fiscal Period ."
        data-type="date"
        required="false"
        display="true" />

      <attribute
        name ="Display Month of Year"
        definition="Index of the calendar month (1..12) for the month used in the fiscal period label."
        class="integer" 
        data-type="int" 
        required="true"
        display="true" />

  </entity>

  <entity
    name="Sales Order Line Status History"
    definition="Holiday dates specified for a legal entity."
    class="history" physical="Sales Order Line Status .">

      <!-- GRAIN attributeS -->
      <attribute
        name ="Sales Order Line"
        grain="true"
        class="reference" physical="Sales Order Line ." 
        data-type="text" data-length="200"
        required="true" />

      <attribute
        name ="Status Date"
        definition ="Date for which the sales order line status is specified."
        grain="true"
        class="date" physical="Status ."
        data-type="date" 
        required="true" />

      <!-- FOREIGN KEY attributeS -->
      <attribute
        name ="Sales Order Line Status"
        definition ="Status of the sales order line at the close of the status date."
        class="reference" physical="Sales Order Line Status ."
        data-type="text" data-length="200" 
        display="true" />
      
      <!-- ATTRIBUTE attributeS -->
      <attribute
        name ="Status Comment"
        definition="Date of the first day in the corresponding fiscal period."
        class="description" physical="Status Comment ."
        data-type="text" data-length="200"
        display="true" />

  </entity>

</model>

